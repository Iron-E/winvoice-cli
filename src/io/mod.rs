//! # Summary
//!
//! This module contains other sub-modules which correspond to I/O activities necessary to interact
//! with the `clinvoice` binary. As expected, [`input`](crate::io::input) is for `stdin` and
//! [`output`](crate::io::output) is for `stdout`.

pub mod input;
pub mod output;
